apiVersion: v1
kind: Service
metadata:
  name: kong
  namespace: ecommerce-prod
spec:
  selector:
    app: kong
  ports:
    - name: proxy
      port: 80
      targetPort: 8000
    - name: admin
      port: 8001
      targetPort: 8001
  type: LoadBalancer
